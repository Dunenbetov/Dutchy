# Monolith from repo root when Railway Root Directory is apps/backend.
[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.install]
cmds = ["cd ../.. && npm ci"]

[phases.build]
cmds = ["cd ../.. && npm run build:monolith"]

[start]
cmd = "cd ../.. && npm run start:monolith"
